export interface TextbookData {
  id?: string;
  title: string;
  pdfUrl: string;
  fileName: string;
  fileSize: number;
  uploadedAt: Date;
}

export interface NotesData {
  id?: string;
  title: string;
  pdfUrl: string;
  fileName: string;
  fileSize: number;
  uploadedAt: Date;
}

